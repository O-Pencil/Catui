# 2026-05-13 — Daily Pencil Review (BOOTSTRAP DRY-RUN)

```yaml
status: ok
mode: bootstrap-dry-run    # one-off, extended window; NOT the recurring 24h window
window_start: 2026-04-29T00:00:00Z
window_end:   2026-05-13T23:59:59Z
policy_version: 1
clusters_analyzed: 7
auto_fixes:   0
tickets:      5
observations: 1
bursts:       0
note:         AUTO-FIX execution deferred — first real schedule fire will pick this up
```

> This is a bootstrap dry-run of the SOP (`../daily-issue-sop.md`) covering all 11 events currently in `pencil_issue_events`. Real recurring runs use a 24-hour window per §2.

## Action summary

| Action    | Count | Notes |
|-----------|-------|-------|
| BLOCK     | 0     | — |
| REVIEW    | 5     | 3× mem-core LLM fallbacks, 2× sal.eval telemetry write side |
| AUTO-FIX  | 0     | 1 candidate (`soul.store:loadMemory:parse`) deferred — see Notes |
| OBSERVE   | 1     | `node.warning:max-listeners-exceeded`, single occurrence, warning |

## Auto-fixes applied

_None._ The single AUTO-FIX candidate is held back during the dry-run (see §Notes).

## Issue tickets filed

| Fingerprint | Severity | Class | Ticket | Rationale |
|-------------|----------|-------|--------|-----------|
| `mem-core.extract:fallback:work-llm-failure` | warning | REVIEW | [link](./2026-05-13/mem-core-extract-fallback-work-llm-failure.md) | LLM prompt + retry policy in `packages/mem-core/src/extraction.ts` — SOP §3.3 prompt-template rule |
| `mem-core.extract:fallback:llm-failure` | warning | REVIEW | [link](./2026-05-13/mem-core-extract-fallback-llm-failure.md) | Same code path, different fingerprint — SOP §3.3 prompt-template rule |
| `mem-core.insights:fallback:non-json-llm-output` | warning | REVIEW | [link](./2026-05-13/mem-core-insights-fallback-non-json-llm-output.md) | Insights extraction parser fallback in `packages/mem-core/src/extension.ts` — SOP §3.3 prompt-template rule |
| `sal.eval:network:http-400-PGRST102` | error | REVIEW | [link](./2026-05-13/sal-eval-network-http-400-pgrst102.md) | Telemetry write contract in `extensions/defaults/sal/eval/insforge-sink.ts` — SOP §3.3 telemetry-write-side rule |
| `sal.eval:network:network-error` | error | REVIEW | [link](./2026-05-13/sal-eval-network-network-error.md) | Telemetry transport reliability — SOP §3.3 telemetry-write-side + network-contract rules |

## Observation watchlist

| Fingerprint | Today | 7d avg | Note |
|-------------|-------|--------|------|
| `node.warning:max-listeners-exceeded` | 1 | n/a (first seen) | Single occurrence, warning, emission site is `main.ts` (core boundary) but root cause unknown — would BLOCK on any fix that touches `main.ts`. Re-evaluate if it recurs. |

## Burst detector

_No bursts._ All 7 clusters in this bootstrap have rates ≤ baseline and span 14 days.

## Notes

1. **Window mode.** This is `bootstrap-dry-run`, not the normal 24-hour pass. The recurring run reads `now() - interval '24 hours'` per SOP §2.1; today that window is empty (most recent event: 2026-05-11T17:48Z).
2. **AUTO-FIX deferral.** `soul.store:loadMemory:parse` qualifies as AUTO-FIX under §3.4 (single file, NUL-byte parse fallback, schema-preserving). The candidate change would add a corruption back-up step before resetting the file. Held back during the dry-run; the first real schedule fire will pick it up if the cluster is still present in its 24-hour window. (At the current rate it likely won't recur — that's also useful signal.)
3. **mem-core LLM fallback pattern.** Three out of seven clusters are LLM-returns-non-JSON in mem-core. Different fingerprints, same family. A real fix here likely changes (a) the prompt asking for JSON, (b) the retry policy, or (c) the parser's tolerance — all are §3.3 REVIEW territory. The three tickets share many suggested options; if a human picks one approach, the others fold in.
4. **SAL telemetry reliability.** Two clusters from the SAL eval insforge sink. PGRST102 + `socket hang up` together suggest endpoint flakiness or a request-body encoding bug. Both REVIEW because any fix lands on the write side of `pencil_issue_events`'s sister tables.
5. **Schedule liveness caveat.** The recurring cron `3 9 * * *` (local) is session-bound in this Claude session; it expires after 7 days and dies if the session ends. To make it durable across machine restarts, the SOP could later be wrapped in a `scripts/daily-pencil-review.sh` that the user wires to system cron — out of scope for this dry-run.
