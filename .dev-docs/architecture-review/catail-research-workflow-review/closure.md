# CATAIL Research Workflow Review Closure

Status: closed — V1 accepted, V2 validated, and V3 professional-scientific-agent repositioning accepted locally.

The review accepts an extension-owned, passive, default-discovered CATAIL V1. The implementation adds the routing Skill, progressively loaded playbooks, artifact templates and contracts, a dependency-free read-only structural audit, default resource discovery, and focused regression coverage.

## Acceptance evidence

- `verify:dip`: passed, 627/627 source files with valid P3 headers and 34 P2 modules checked.
- `verify:quality`: passed, 700 TypeScript files scanned.
- `verify:package-boundary`: passed; the post-build dist boundary check also passed.
- `build`: passed; CATAIL Skill, references, templates, and audit script are present in `dist/extensions/builtin/catail/`.
- `tsc --noEmit`: passed after the required internal dependency build.
- Focused default-extension/CATAIL/discipline suite: 17/17 tests passed.
- Skill Creator validation: passed.

Native research controllers, automatic experiment execution, network services, reviewer dispatch, and public protocol changes remain intentionally deferred. Reopen only under the usage conditions in CRW01.

## V2 corrective scope

CRW02 reopens the review after a real workflow produced a detailed study design before any literature source was retained or the topic's contribution and feasibility were positioned. V2 adds source, hypothesis, position, and iteration artifacts plus cumulative audit gates. Closure returns to `closed` only after the demonstrated failure case and complete lifecycle fixture pass alongside all repository gates.

## V2 acceptance evidence

- The complete positioned research fixture passes the write-stage audit.
- Missing `POSITION.md` blocks design, and `pilot-only` rejects a confirmatory study role.
- The demonstrated `research/` workspace fails design with zero searches, zero retained sources, no hypothesis register, and no position record.
- Focused CATAIL tests: 6/6 passed.
- Skill Creator validation: passed.
- DIP, quality, package-boundary, build, and `tsc --noEmit`: passed.
- No new runtime controller, network call, dependency, public API, or cross-extension import was added.

## V3 repositioning scope

CRW03 changes the user-facing identity from an auditable research workflow to CATAIL — Professional Scientific Agent. It adds explicit name-only activation, composable scientific methods, shared lifecycle and decision gates, epistemic-state reasoning, and independent dialogue/artifact/source/terminology language scopes. V2's position-before-design and negative-result protections remain cross-cutting stewardship.

V3 closure requires focused activation and method-catalog tests, Skill Creator validation, repository acceptance gates, and confirmation that no dedicated slash command, runtime controller, public API, dependency, or automatic workspace side effect was added.

## V3 acceptance evidence

- Standard invocation remains `/skill:catail`; the extension registers no dedicated slash command.
- The bootstrap and Skill description require explicit CATAIL naming and prohibit keyword-based activation.
- The scientific-method catalog documents nine selectively composable modes, and all Skill-linked playbooks resolve.
- Task routing preserves endpoints and stop boundaries; dialogue, artifact, source, and terminology language scopes remain independent.
- Focused CATAIL tests: 7/7 passed.
- Skill Creator validation: passed under UTF-8 mode.
- `verify:dip`: passed, 627/627 P3 source headers and 34 P2 modules checked.
- `verify:quality`: passed, 700 TypeScript files scanned.
- `verify:package-boundary` and `verify:package-boundary:dist`: passed.
- `build`: passed; all three foundation references are present in `dist/extensions/builtin/catail/references/foundations/`.
- `tsc --noEmit`: passed.
- No command, controller, dependency, public API, timer, network action, or automatic workspace write was added.
