# BC01: Preserve both evolution evidence contracts

status: resolved in integration
owner: extensions/optional/evolution/

Source repair uses frozen executable regressions, exact commits and artifact
integrity. Declarative promotion uses paired held-out task reports bound to
candidate content. Both improve the earlier replay-only acceptance, but address
different artifacts. Preserve both gates without treating either as a replacement
for the other. Historical RE01 wording must describe the intake state rather than
claiming that the integrated declarative gate still ignores its candidate.

Acceptance: both evolution suites and the critical harness pass on their combined
tree; no public protocol expansion or cross-extension dependency is necessary.
